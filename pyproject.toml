[project]
name = "parselqcdata"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "starlingnia", email = "rahalinedophy@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "polars>=1.44.2",
]

[project.scripts]
parselqcdata = "parselqcdata:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
